# Data directory

Market data is **not** stored in git (too large).

Canonical external path (LaCie):

```
/Volumes/LaCie/Aether/
```

Local pointers:
- `data/databento` → symlink to LaCie databento root when configured
- `data/fmp/` → active FMP download workdir (gitignored)

See `docs/DATA_STATUS.md`.
